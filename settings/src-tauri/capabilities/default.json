{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default HashOverlay settings window permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
